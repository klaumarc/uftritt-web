---
import Lottie from "./Lottie.astro";

const nav = [
  { label: "Agentur", href: "/agentur" },
  {
    label: "Angebot",
    href: "/angebot",
    children: [
      { label: "Grafikdesign", href: "/angebot/grafikdesign" },
      { label: "Webdesign & Entwicklung", href: "/angebot/webdesign" },
      { label: "Videoproduktion", href: "/angebot/videoproduktion" },
      { label: "Applikationsentwicklung", href: "/angebot/applikationsentwicklung" },
      { label: "Marketingbegleitung", href: "/angebot/marketingbegleitung" },
      { label: "Magazinbetreuung", href: "/angebot/magazinbetreuung" },
    ],
  },
  { label: "Projekte", href: "/projekte" },
  { label: "Partner", href: "/partner" },
  { label: "News", href: "/aktuell" },
  { label: "Kontakt", href: "/kontakt" },
];

const social = [
  { key: "linkedin", label: "LinkedIn", href: "https://www.linkedin.com/company/uftritt-gmbh/", icon: "/icons/linkedin-icon.svg" },
  { key: "instagram", label: "Instagram", href: "https://www.instagram.com/uftritt/", icon: "/icons/instagram-icon.svg" },
  { key: "facebook", label: "Facebook", href: "https://www.facebook.com/profile.php?id=61551877650400", icon: "/icons/facebook-icon.svg" },
  { key: "youtube", label: "YouTube", href: "https://www.youtube.com/@UftrittGmbH", icon: "/icons/youtube-icon.svg" },
];
---

<header class="site-header">
  <div class="container header-inner">
    <a href="/" class="header-logo" aria-label="Uftritt Startseite" data-lottie-trigger>
      <Lottie src="/lottie/logo.json" hover={true} ariaLabel="Uftritt" />
    </a>

    <button
      class="menu-trigger"
      type="button"
      aria-haspopup="dialog"
      aria-expanded="false"
      aria-controls="menu-overlay"
    >
      Menü
    </button>

    <ul class="header-social" aria-label="Social Media">
      {
        social.map((s) => (
          <li>
            <a href={s.href} target="_blank" rel="noopener" aria-label={s.label} class={`social--${s.key}`}>
              <img src={s.icon} alt="" />
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</header>

{/* ---------- Vollbild-Menü ---------- */}
<div class="menu-overlay" id="menu-overlay" role="dialog" aria-modal="true" aria-label="Menü" hidden>
  <div class="container menu-overlay-top">
    <a href="/" class="header-logo" aria-label="Uftritt Startseite">
      <img src="/logo/uftritt-blau.svg" alt="Uftritt" width="130" height="42" />
    </a>
    <button class="menu-close" type="button" aria-label="Menü schliessen">
      <svg viewBox="0 0 32 32" width="32" height="32" aria-hidden="true">
        <path d="M6 6l20 20M26 6L6 26" stroke="currentColor" stroke-width="4" stroke-linecap="round" />
      </svg>
    </button>
  </div>

  <nav class="menu-nav" aria-label="Hauptnavigation">
    <ul>
      {nav.map((item, i) => (
        <li style={`--i:${i}`} class={item.children ? "has-sub" : undefined}>
          <a href={item.href}>
            <span class="m-arrow" aria-hidden="true"><i></i></span><span class="m-label">{item.label}</span>
          </a>
          {item.children && (
            <ul class="submenu" aria-label={`${item.label} – Dienstleistungen`}>
              {item.children.map((c) => (
                <li>
                  <a href={c.href}>
                    <span class="m-arrow" aria-hidden="true"><i></i></span><span class="m-label">{c.label}</span>
                  </a>
                </li>
              ))}
            </ul>
          )}
        </li>
      ))}
    </ul>
    <ul class="menu-social">
      {social.map((s, i) => (
        <li style={`--i:${nav.length + i}`}>
          <a href={s.href} target="_blank" rel="noopener">
            <span class="m-arrow" aria-hidden="true"><i></i></span><span class="m-label">{s.label}</span>
          </a>
        </li>
      ))}
    </ul>
  </nav>
</div>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--color-yellow);
  }
  .header-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    min-height: 76px;
  }
  .header-logo {
    display: block;
    width: 132px;
    height: 42px;
  }
  .header-logo img {
    height: 42px;
    width: auto;
  }

  .menu-trigger {
    margin-inline: auto;
    background: none;
    border: none;
    cursor: pointer;
    font-family: var(--font-base);
    font-weight: 700;
    font-size: 1.15rem;
    color: var(--color-blue);
    padding: 0.4rem 0.6rem;
    position: relative;
  }
  .menu-trigger::after {
    content: "";
    position: absolute;
    left: 0.6rem;
    right: 0.6rem;
    bottom: 0.1rem;
    height: 2px;
    background: var(--color-blue);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.42s var(--ease);
  }
  .menu-trigger:hover::after {
    transform: scaleX(1);
  }

  .header-social {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }
  .header-social a {
    display: grid;
    place-items: center;
    width: 24px;
    height: 26px;
    transition: transform 0.32s var(--ease);
  }
  .header-social a:hover {
    transform: translateY(-3px);
  }
  /* Optisch ausgeglichene Icon-Grössen (unterschiedliche viewBox-Proportionen) */
  .header-social img {
    display: block;
    width: auto;
  }
  .social--linkedin img,
  .social--instagram img {
    height: 18px;
  }
  .social--facebook img {
    height: 17px; /* schmales, hohes "f" – etwas kleiner, damit es nicht dominiert */
  }
  .social--youtube img {
    height: 15px; /* breites Icon – über Höhe begrenzt */
  }

  /* ---------- Overlay ---------- */
  .menu-overlay {
    position: fixed;
    inset: 0;
    z-index: 200;
    background: var(--color-yellow);
    display: flex;
    flex-direction: column;
    opacity: 0;
    transition: opacity 0.55s var(--ease);
  }
  .menu-overlay.is-open {
    opacity: 1;
  }
  .menu-overlay[hidden] {
    display: none;
  }
  .menu-overlay-top {
    position: relative;
    display: flex;
    align-items: center;
    min-height: 76px;
  }
  /* X erscheint an derselben Stelle wie der "Menü"-Trigger (oben zentriert) */
  .menu-close {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-blue);
    padding: 0.5rem;
    transition: transform 0.4s var(--ease);
  }
  .menu-close:hover {
    transform: translate(-50%, -50%) rotate(90deg);
  }

  .menu-nav {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: safe center;
    align-items: flex-start;
    text-align: left;
    width: 100%;
    /* linksbündig, aber Block beginnt ~Bildschirmmitte + 10% (≈60%) */
    padding-left: 58vw;
    padding-right: var(--gutter);
    padding-block: 0 8vh;
    overflow-y: auto;
  }
  @media (max-width: 820px) {
    .menu-nav {
      padding-left: var(--gutter);
    }
  }
  .menu-nav > ul li a {
    display: inline-flex;
    align-items: center;
    font-weight: 700;
    line-height: 1.15;
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    color: var(--color-blue);
  }
  /* Marken-Pfeil gleitet bei Hover links rein, Label rückt nach */
  .m-arrow {
    display: inline-block;
    max-width: 0;
    margin-right: 0;
    overflow: visible; /* Pfeil bleibt beim Rausgleiten nach links sichtbar */
    transition: max-width 0.6s var(--ease), margin-right 0.6s var(--ease);
  }
  .m-arrow i {
    display: block;
    width: 0.95em;
    height: 0.72em;
    background-color: var(--color-blue);
    -webkit-mask: url("/icons/pfeil-rechts-blau.svg") no-repeat center / contain;
    mask: url("/icons/pfeil-rechts-blau.svg") no-repeat center / contain;
    opacity: 0;
    transform: translateX(-16px);
    transition: opacity 0.5s var(--ease), transform 0.6s var(--ease);
  }
  .menu-nav > ul li a:hover .m-arrow,
  .menu-nav > ul li a:focus-visible .m-arrow {
    max-width: 0.95em;
    margin-right: 0.45em;
  }
  .menu-nav > ul li a:hover .m-arrow i,
  .menu-nav > ul li a:focus-visible .m-arrow i {
    opacity: 1;
    transform: translateX(0);
  }
  .menu-nav > ul {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: clamp(0.6rem, 1.8vw, 1.4rem);
  }
  .menu-nav > ul > li {
    position: relative;
  }

  /* ---------- Submenu (Dienstleistungen, rechts neben "Angebot") ---------- */
  .submenu {
    position: absolute;
    left: 100%;
    top: 50%;
    /* Padding statt Margin: überbrückt die Lücke als Hover-Fläche */
    padding-left: clamp(1.5rem, 3vw, 3.5rem);
    display: flex;
    flex-direction: column;
    gap: clamp(0.25rem, 0.8vw, 0.6rem);
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translateY(-50%) translateX(10px);
    transition: opacity 0.45s var(--ease), transform 0.6s var(--ease);
  }
  .menu-nav > ul > li.has-sub:hover .submenu,
  .menu-nav > ul > li.has-sub:focus-within .submenu {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translateY(-50%) translateX(0);
  }
  .menu-nav .submenu li a {
    font-size: clamp(0.95rem, 1.4vw, 1.25rem);
    font-weight: 700;
    line-height: 1.2;
  }
  /* "Angebot" selbst zeigt keinen Pfeil – der Hover öffnet stattdessen das Submenu */
  @media (min-width: 821px) {
    .menu-nav > ul > li.has-sub > a:hover .m-arrow,
    .menu-nav > ul > li.has-sub > a:focus-visible .m-arrow {
      max-width: 0;
      margin-right: 0;
    }
    .menu-nav > ul > li.has-sub > a:hover .m-arrow i,
    .menu-nav > ul > li.has-sub > a:focus-visible .m-arrow i {
      opacity: 0;
      transform: translateX(-16px);
    }
  }
  /* Mobile: Submenu weglassen – "Angebot" bleibt ein normaler Link mit Pfeil */
  @media (max-width: 820px) {
    .submenu {
      display: none;
    }
  }
  .menu-nav > ul.menu-social {
    margin-top: 2.75rem;
    gap: 0.05rem;
  }
  .menu-nav > ul.menu-social li a {
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--color-blue);
  }
  .menu-nav > ul.menu-social li a:hover {
    text-decoration: none;
  }

  /* Stagger-Einblendung der Menüpunkte */
  .menu-overlay .menu-nav li {
    opacity: 0;
    transform: translateY(18px);
  }
  .menu-overlay.is-open .menu-nav li {
    animation: menuIn 0.5s var(--ease) forwards;
    animation-delay: calc(120ms + var(--i, 0) * 60ms);
  }
  @keyframes menuIn {
    to { opacity: 1; transform: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .menu-overlay,
    .menu-overlay .menu-nav li {
      transition: none;
      animation: none;
      opacity: 1;
      transform: none;
    }
  }
</style>

<script>
  const trigger = document.querySelector<HTMLButtonElement>(".menu-trigger");
  const overlay = document.querySelector<HTMLElement>("#menu-overlay");
  const closeBtn = overlay?.querySelector<HTMLButtonElement>(".menu-close");
  const links = overlay?.querySelectorAll<HTMLAnchorElement>("a");

  function openMenu() {
    if (!overlay) return;
    overlay.hidden = false;
    // reflow, damit die opacity-Transition greift
    void overlay.offsetWidth;
    overlay.classList.add("is-open");
    document.body.style.overflow = "hidden";
    trigger?.setAttribute("aria-expanded", "true");
    closeBtn?.focus();
  }
  function closeMenu() {
    if (!overlay) return;
    overlay.classList.remove("is-open");
    document.body.style.overflow = "";
    trigger?.setAttribute("aria-expanded", "false");
    const onEnd = () => {
      overlay.hidden = true;
      overlay.removeEventListener("transitionend", onEnd);
    };
    overlay.addEventListener("transitionend", onEnd);
    trigger?.focus();
  }

  trigger?.addEventListener("click", openMenu);
  closeBtn?.addEventListener("click", closeMenu);
  links?.forEach((l) => l.addEventListener("click", closeMenu));
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && overlay && !overlay.hidden) closeMenu();
  });
</script>
